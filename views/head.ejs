<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="color-scheme" content="dark light">
    <title>
      <%= typeof pageName == "undefined" ? siteName : `${pageName} - ${siteName}` -%>
    </title>
    <link rel="stylesheet" href="/style.css">
  </head>
  <body>
    <%- include("header") -%>
    <main>